<% if current_user.admin %>
  <hr>
  <div> <%= link_to t('shared.link_admin_version'), staff_docs_path %> </div>
  <hr>
<% end %>

<%= link_to t('shared.back'), :back %>
<hr>

<h3> <%= t('docs.index.title-phrase-1') %> </h3>
<br>
<h4> <%= t('docs.index.title-phrase-2') %> </h4>
<br>
<hr>

<div>
  <%= t('docs.index.docs-count', data: @count_phrase) %>
</div>
<br>

<div>
  <% docs.each do |doc| %>
    <ul>
      <li> <%= link_to doc.title.titleize, login_doc_path(doc) %> </li>
    </ul>
  <% end %>
</div>

<br>
<hr>
<p> <%= link_to t('staff.doc_links.new.link-challenge-answer'), new_login_doc_link_path %> </p>